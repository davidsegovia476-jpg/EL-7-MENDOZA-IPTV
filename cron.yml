jobs:
  actualizar-lista:
    runs-on: ubuntu-latest
    timeout-minutes: 5 # 👈 Si se queda trabado en amarillo, GitHub lo frena a los 5 minutos

    steps:
    - name: Clonar repositorio
# ... (el resto del código del archivo cron.yml sigue exactamente igual)

